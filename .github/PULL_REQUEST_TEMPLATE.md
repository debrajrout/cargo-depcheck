## What does this PR do?

<!-- One or two sentences. Link any related issue with "Fixes #123". -->

## How was this tested?

<!-- e.g. `cargo test`, ran `cargo depcheck` against a real project, added a fixture in tests/fixtures/ -->

## Checklist

- [ ] `cargo test` passes
- [ ] `cargo clippy` is clean
- [ ] `cargo fmt` applied
- [ ] Updated README/docs if behavior or CLI flags changed
