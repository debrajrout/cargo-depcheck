/target
# Fixture crates under tests/fixtures/ each build their own target/ if
# `cargo build`/`test` is ever run directly against them.
tests/fixtures/*/target
*.pdb

# advisory DB cache — fetched at runtime, no need to track
.advisory-db/

# Local LLM / IDE context (not for publication)
HANDOVER.md
# AGENTS.md
# CLAUDE.md
# .cursor/
# .claude/

# OS / editor noise
.DS_Store
*.swp
*~
IMPLEMENTATION_PLAN.md

# Python bytecode from tools/
__pycache__/
*.pyc
